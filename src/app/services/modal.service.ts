import {  Injectable } from '@angular/core';
//import { NzModalService } from 'ng-zorro-antd/modal';

@Injectable({
  providedIn: 'root'
})
export class ModalService {
  /* constructor(private modal: NzModalService) {} // Aquí se inyecta correctamente


  // Método para abrir un modal
  openModal(title: string, content: string): void {
    this.modal.create({
      nzTitle: title,
      nzContent: content,
      nzClosable: true,
      nzFooter: null, // Para que no tenga botones por defecto
    });
  }

  openComponentModal(): void {
    this.modal.create({
      nzTitle: 'Modal con Componente',
      nzContent: CustomModalComponent,
      nzClosable: true,
      nzFooter: null,
    });
  }
   */
}
